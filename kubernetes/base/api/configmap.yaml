apiVersion: v1
kind: ConfigMap
metadata:
  name: api-config
  labels:
    app.kubernetes.io/name: api
    app.kubernetes.io/part-of: platform
data:
  NODE_ENV: "production"
  PORT: "3000"
  QUEUE_NAME: "platform-jobs"
  LOG_LEVEL: "info"
